[project]
name = "cutleast-core-lib"
version = "1.3.1"
description = "A core library used by my projects"
authors = [{ name = "cutleast", email = "cutleast@gmail.com" }]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "comtypes ; sys_platform == 'win32'",
    "darkdetect",
    "jstyleson",
    "material-color-utilities",
    "py7zr",
    "pydantic",
    "pyside6",
    "qtawesome",
    "rarfile",
    "requests",
    "semantic-version",
    "virtual-glob",
]

[build-system]
requires = ["hatchling", "pyside6"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cutleast_core_lib"]

[tool.hatch.build.targets.sdist]
include = ["/res", "/scripts", "/src/cutleast_core_lib", "/hatch_build.py"]

[tool.hatch.build.hooks.custom]
path = "hatch_build.py"

[dependency-groups]
dev = [
    "pyfakefs",
    "pyright",
    "pytest",
    "pytest-cov",
    "pytest-mock",
    "pytest-qt",
    "requests-mock",
    "ruff",
    "types-requests",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "tests"]
log_cli = true
log_cli_level = "DEBUG"
log_cli_format = "[%(asctime)s.%(msecs)03d][%(levelname)s][%(name)s.%(funcName)s]: %(message)s"

[tool.pyright]
include = ["src", "tests"]
typeCheckingMode = "strict"
exclude = ["**/__pycache__", "**/*.old", "src/cutleast_core_lib/ui/resources_rc.py"]
stubPath = "./stubs"
extraPaths = ["src"]

# Check here for a full list of options: https://github.com/microsoft/pyright/blob/main/docs/configuration.md#diagnostic-settings-defaults
deprecateTypingAliases = false
reportImplicitOverride = "warning"
reportMatchNotExhaustive = false
reportMissingTypeArgument = false
reportMissingTypeStubs = false
reportPropertyTypeMismatch = "warning"
reportShadowedImports = "warning"
reportUnknownArgumentType = false
reportUnknownLambdaType = false
reportUnknownMemberType = false
reportUnknownParameterType = false
reportUnknownVariableType = false
reportUnnecessaryCast = "information"
reportUnnecessaryComparison = "information"
reportUnnecessaryContains = "information"
reportUnnecessaryIsInstance = "information"
reportUnnecessaryTypeIgnoreComment = "information"
reportUnusedClass = "warning"
reportUnusedImport = "warning"
reportUnusedFunction = "warning"
reportUnusedVariable = "warning"
